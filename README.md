# ClearAller
Personalized Allergen Transparency System

## Frontend (ClearAller Vision / AllerGuard)

The production-ready mobile-first web frontend lives in:

- `clearaller-vision/`

Run it:

```bash
cd clearaller-vision
npm install
npm run dev
```
